import 'package:flutter/material.dart';

class CourseMain extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('CourseMain'),
    );
  }
}
