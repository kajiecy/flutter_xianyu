import 'package:flutter/material.dart';

class ConsultMain extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('ConsultMain'),
    );
  }
}
